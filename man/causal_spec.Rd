% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_spec.R
\name{causal_spec}
\alias{causal_spec}
\title{Create a Causal Problem Specification}
\usage{
causal_spec(
  data,
  treatment,
  outcome,
  covariates = NULL,
  negative_control = NULL,
  instrument = NULL,
  estimand = c("ATE", "ATT", "ATC"),
  outcome_type = c("continuous", "binary", "count"),
  na.action = na.omit
)
}
\arguments{
\item{data}{A data.frame or data.table containing the analysis data}

\item{treatment}{Character: name of the treatment variable}

\item{outcome}{Character: name of the outcome variable}

\item{covariates}{Character vector: names of adjustment covariates (NULL for none)}

\item{negative_control}{Character: name of negative control outcome (optional)}

\item{instrument}{Character: name of instrumental variable (optional)}

\item{estimand}{Character: target estimand, one of "ATE", "ATT", or "ATC"}

\item{outcome_type}{Character: type of outcome, one of "continuous", "binary", "count"}

\item{na.action}{Function: how to handle missing values (default: na.omit)}
}
\value{
Object of class "causal_spec".
\strong{Note}: While \code{causal_spec} can describe categorical or continuous treatments,
current downstream estimation functions (\code{estimate_deficiency}, \code{estimate_effect})
require \strong{binary} treatments.
}
\description{
Defines the causal inference problem including treatment, outcome, covariates,
and optional diagnostic variables.
}
\examples{
# Create sample data
n <- 200
W <- rnorm(n)
A <- rbinom(n, 1, plogis(0.5 * W))
Y <- 1 + 2 * A + W + rnorm(n)
df <- data.frame(W = W, A = A, Y = Y)

# Create causal specification
spec <- causal_spec(
  data = df,
  treatment = "A",
  outcome = "Y",
  covariates = "W"
)

print(spec)

}
\seealso{
\code{\link[=estimate_deficiency]{estimate_deficiency()}}, \code{\link[=nc_diagnostic]{nc_diagnostic()}}, \code{\link[=policy_regret_bound]{policy_regret_bound()}}
}
