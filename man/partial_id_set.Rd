% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theorem_bounds.R
\name{partial_id_set}
\alias{partial_id_set}
\title{Partial Identification Set from a Delta Radius (Result 8)}
\usage{
partial_id_set(
  estimate,
  delta,
  estimand = c("ate", "mean"),
  outcome_range = c(0, 1)
)
}
\arguments{
\item{estimate}{Point estimate of the functional (numeric).}

\item{delta}{Deficiency radius \eqn{\delta \in [0,1]}.}

\item{estimand}{One of \code{"mean"} or \code{"ate"}.}

\item{outcome_range}{Numeric vector c(min, max) bounding the outcome.}
}
\value{
List with \code{lower}, \code{upper}, \code{estimate}, and \code{half_width}.
}
\description{
Converts a deficiency radius \eqn{\delta} into a conservative identified interval
for bounded functionals under TV.
}
\details{
For a mean functional with \eqn{Y \in [y_{min}, y_{max}]}, TV control yields
\eqn{|E_Q[Y] - E_{Q_0}[Y]| \le 2\delta (y_{max}-y_{min})}.
For an ATE (difference of two means), the half-width doubles again.
}
